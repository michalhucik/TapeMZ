# Test Data

The `tstdata/` directory originally contained `.wav` files used by the analyzer.

However, GitHub has limits on large file sizes. Therefore, the test data has been packaged into a separate archive.

You can download it here:
https://ordoz.com/TapeMZ/tstdata/
